#include "main.h"
#include <stdlib.h>
/** 
  *legizew mnm aynet descreption antsaf
  */
int _printf(const char *format, ...)
{
 va_list args;
	int i = 0;

	va_start(args, format);
	while (format && format[i])
	{
  i++;
 }
}
